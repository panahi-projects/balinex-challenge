/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.google.com",
      "assets.coincap.io",
      "assets.coingecko.com",
      "coin-images.coingecko.com",
    ],
  },
};

module.exports = nextConfig;
